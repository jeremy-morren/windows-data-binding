<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <IsRoslynComponent>true</IsRoslynComponent>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>

    <PackageId>WinDataBinding</PackageId>
    <Description>
      A source generator that flattens deep object graphs into bindable models for WPF and WinForms.
    </Description>

    <!-- The generator ships as an analyzer, not as a lib; the attributes assembly is packed by hand below. -->
    <IncludeBuildOutput>false</IncludeBuildOutput>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="5.9.0" PrivateAssets="all" ExcludeAssets="runtime" />
    <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="5.9.0-1.26328.17" PrivateAssets="all" ExcludeAssets="runtime" />

    <!-- PrivateAssets so the package declares no dependency on it; the assembly itself is packed instead. -->
    <ProjectReference Include="..\WinDataBinding.SourceGenerator.Attributes\WinDataBinding.SourceGenerator.Attributes.csproj" PrivateAssets="All" />
  </ItemGroup>

  <ItemGroup>
    <!-- The generator, and the attributes it needs to see while analysing. -->
    <None Include="$(OutputPath)$(AssemblyName).dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
    <None Include="$(OutputPath)WinDataBinding.SourceGenerator.Attributes.dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />

    <!--
      The same attributes as a normal reference, so consuming code can apply them. This is a real runtime
      dependency, not just a compile-time one: reading an attribute back reflectively loads the assembly
      that declares it, so it has to reach the consumer's output. The docs travel with it for IntelliSense.
    -->
    <None Include="$(OutputPath)WinDataBinding.SourceGenerator.Attributes.dll" Pack="true" PackagePath="lib/netstandard2.0" Visible="false" />
    <None Include="$(OutputPath)WinDataBinding.SourceGenerator.Attributes.xml" Pack="true" PackagePath="lib/netstandard2.0" Visible="false" />
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Include="AnalyzerReleases.Shipped.md" />
    <AdditionalFiles Include="AnalyzerReleases.Unshipped.md" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="WinDataBinding.SourceGenerator.Tests" />
  </ItemGroup>

</Project>
